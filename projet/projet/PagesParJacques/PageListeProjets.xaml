<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="projet.PagesParJacques.PageListeProjets"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:projet.PagesParJacques"
    xmlns:local1="using:projet.ClassesParJacques"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0">
            <TextBlock FontSize="50" FontWeight="ExtraBlack">Projets</TextBlock>
        </StackPanel>

        <GridView x:Name="listeProjets" Grid.Row="1" SelectionChanged="listeProjets_SelectionChanged">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local1:Projet">
                    <StackPanel Orientation="Vertical" Width="200" Spacing="10" Background="Firebrick" Margin="5" Padding="5">
                        <TextBlock Text="{x:Bind Numero}" FontWeight="ExtraBlack" FontSize="30" Foreground="LightCoral"/>
                        <TextBlock Text="{x:Bind Titre}" FontWeight="ExtraBlack" FontSize="30" Foreground="LightCoral"/>
                        <TextBlock Text="{x:Bind NomClient}" FontSize="25" Foreground="LightCoral"/>
                        <TextBlock Text="{x:Bind Budget}" FontWeight="ExtraBlack" FontSize="30" Foreground="LightCoral"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
