<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="projet.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:projet"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="projet">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <NavigationView Grid.Row="1" PaneDisplayMode="Left" PaneTitle="Gestion d'une entreprise" x:Name="navView" Background="Aqua" ItemInvoked="navView_ItemInvoked" IsBackButtonVisible="Visible"  IsBackEnabled="True" BackRequested="navView_BackRequested" IsSettingsVisible="False">

            <NavigationView.MenuItems>
                <NavigationViewItemHeader Content="Compte Administrateur"/>
                <NavigationViewItem Content="Employes" x:Name="iEmploye" Icon="Contact" Tag="employes"/>
                <NavigationViewItem Content="Clients" x:Name="iClients" Icon="People" Tag="clients"/>
                <NavigationViewItemSeparator />
                <NavigationViewItem Content="Projets" x:Name="iProjets" Icon="Document" Tag="projets"/>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Déconnexion" x:Name="iDeconnexion" Icon="Switch" Tag="deconnexion" />
                <NavigationViewItem Content="Aide" x:Name="iAide" Icon="Help" Tag="aide"/>
                <NavigationViewItem Content="A propos" x:Name="iAPropos" Icon="Help" Tag="apropos"/>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="mainFrame" Grid.Row="1"/>
        </NavigationView>

    </Grid>
</Window>
