<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="projet.pages.PageListeProjet"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:projet.pages"
    xmlns:local1="using:projet.classes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="50" FontWeight="ExtraBlack">Listes des Projets</TextBlock>
            <Button x:Name="btnCsvExport" Content="Exporter tout en CSV" Click="btnCsvExport_Click" Background="DarkGreen" HorizontalAlignment="Center"/>

            <CommandBar Background="Transparent" IsOpen="False" DefaultLabelPosition="Right">
                <AppBarButton x:Name="appBarAjoutProjet" Icon="Add" Label="Ajouter" Click="appBarAjoutProjet_Click" Tag="ajoutProjet" Background="Gold"/>
            </CommandBar>
        </StackPanel>

        <GridView x:Name="listeProjets" Grid.Row="1" SelectionChanged="listeProjets_SelectionChanged">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local1:Projet">
                    <StackPanel Orientation="Vertical" Width="200" Spacing="10" Background="LightGreen"  Margin="5" Padding="5">
                        <TextBlock Text="{x:Bind Numero}" FontWeight="ExtraBlack" FontSize="7"/>
                        <TextBlock Text="{x:Bind Titre}" FontWeight="ExtraBlack" FontSize="25"/>
                        <TextBlock Text="{x:Bind NomClient}" FontSize="15"/>
                        <TextBlock Text="{x:Bind Budget}" FontWeight="ExtraBlack" FontSize="10"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>


</Page>
