<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="projet.pages.PageAffichEmp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:projet.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local1="using:projet.classes"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock x:Name="tbxTitreEmp" Text="Listes des employés" FontSize="50" FontWeight="Bold"></TextBlock>

            <CommandBar Background="Transparent"  IsOpen="False" DefaultLabelPosition="Right">
                <AppBarButton x:Name="appBarAjoutEmp" Icon="Add" Label="Ajouter" Click="appBarAjoutEmp_Click" Tag="ajoutEmp" Background="Gold"/>
            </CommandBar>
        </StackPanel>
        <GridView x:Name="grEmp" Padding="10"  SelectionChanged="grEmp_SelectionChanged" Grid.Row="1">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local1:Employe">
                    <StackPanel Background="CadetBlue"  Height="200" Width="300">
                        <Grid Height="200" Width="300">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="130"/>
                                <RowDefinition Height="100"/>
                            </Grid.RowDefinitions>
                            <Image x:Name="imgUrlCouverture" Source="{x:Bind Photo}" Stretch="Fill" Grid.Row="0"/>
                            <StackPanel Background="BlanchedAlmond" Orientation="Vertical" Padding="5" Grid.Row="1">
                                <TextBlock x:Name="tblNom"
                                        FontSize="20"
                                        Foreground="Black"
                                        Text="{x:Bind Nom}"
                                />
                                <TextBlock x:Name="tblPrenom"
                                        FontSize="15"
                                        Foreground="Brown"
                                        Text="{x:Bind Prenom}"
                                />

                                <TextBlock x:Name="tblEmail"
                                        FontSize="12"
                                        Foreground="Blue"
                                        Text="{x:Bind Email}"
                                />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

    </Grid>
</Page>
