<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="projet.pages.PageListeClients"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:projet.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local1="using:projet.classes"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock FontSize="50" FontWeight="ExtraBlack">Listes des clients</TextBlock>
            <CommandBar Background="Transparent"  IsOpen="False" DefaultLabelPosition="Right">
                <AppBarButton x:Name="appBarAjoutClient" Icon="Add" Label="Ajouter" Click="appBarAjoutClient_Click" Tag="ajoutEmp" Background="Gold"/>
            </CommandBar>
        </StackPanel>

        <GridView x:Name="listeClient" Grid.Row="1" SelectionChanged="listeClient_SelectionChanged" Padding="10" Background="Transparent">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local1:Client">
                    <StackPanel Orientation="Vertical" Width="250" Background="Blue" CornerRadius="5" Margin="5" Padding="5" Spacing="5">
                        <TextBlock Text="{x:Bind Nom}" FontWeight="ExtraBlack" FontSize="26" Foreground="White"/>
                        <TextBlock Text="{x:Bind Email}" FontSize="18" Foreground="White"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
