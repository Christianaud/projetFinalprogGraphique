<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="projet.pages.PageModifierClient"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:projet.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel x:Name="display" Orientation="Vertical"  Spacing="5" Padding="5" Width="350" Background="LightSkyBlue">
            <TextBlock FontSize="25" FontWeight="ExtraBlack">Modification du Client:</TextBlock>
            <TextBox x:Name="tbxNom" Header="Nom du client"/>
            <TextBlock x:Name="tblNomErreur" Foreground="red" FontSize="15"/>
            <TextBox x:Name="tbxAdresse" Header="adresse"/>
            <TextBlock x:Name="tblAdresseErreur" Foreground="red" FontSize="15"/>
            <TextBox x:Name="tbxNumTel" Header="Numéro de téléphone"/>
            <TextBlock x:Name="tblNumTelErreur" Foreground="red" FontSize="15"/>
            <TextBox x:Name="tbxEmail" Header="Adresse courriel"/>
            <TextBlock x:Name="tblEmailErreur" Foreground="red" FontSize="15"/>
            <Button x:Name="btnModifier" Content="Modifier" Click="btnModifier_Click" Height="45"
                    Background="Orange"
                    Foreground="White"  
                    FontWeight="Bold"
                    HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
