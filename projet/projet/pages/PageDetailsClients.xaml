<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="projet.pages.PageDetailsClients"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:projet.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>

        <StackPanel x:Name="display" Orientation="Vertical" Spacing="15" CornerRadius="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Titre -->
                    <RowDefinition Height="Auto"/>
                    <!-- ID -->
                    <RowDefinition Height="Auto"/>
                    <!-- Nom -->
                    <RowDefinition Height="Auto"/>
                    <!-- Adresse -->
                    <RowDefinition Height="Auto"/>
                    <!-- Téléphone -->
                    <RowDefinition Height="Auto"/>
                    <!-- Email -->
                    <RowDefinition Height="Auto"/>
                    <!-- Bouton -->
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <!-- Labels -->
                    <ColumnDefinition Width="*"/>
                    <!-- Valeurs -->
                </Grid.ColumnDefinitions>

                <!-- Titre -->
                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" FontSize="36" FontWeight="Bold" Foreground="#2E2E2E" Text="Détails du Client" Margin="0,0,0,10"/>

                <!-- ID -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="ID :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="tbxId" Foreground="#555"/>

                <!-- Nom -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Nom :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="tbxNom" Foreground="#555"/>

                <!-- Adresse -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Adresse :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="tbxAdresse" Foreground="#555"/>

                <!-- Téléphone -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Téléphone :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="4" Grid.Column="1" x:Name="tbxNumTel" Foreground="#555"/>

                <!-- Email -->
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Email :" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="5" Grid.Column="1" x:Name="tbxEmail" Foreground="#555"/>

                <!-- Bouton -->
                <Button Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Content="Modifier ce Client"
                HorizontalAlignment="Left" Width="200" Height="45"
                Background="Orange"  Foreground="White" FontWeight="Bold"
                Margin="0,10,0,0"
                Click="btnModifier_Click"/>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
